// Auto-generated by TagTranspiler (GenerateOffset.py)
// Source XML: Lbsp.xml
// Base size: 0x168.

export module Map.Tag.Type:Lbsp.Offset;

import std;

export namespace Map::Tag::Type::Lbsp::Offset
{
    constexpr std::size_t k_Flags                                       = 0x0;  // flags16. [Compressed=0, Xsynced Geometry=1, Relightmapped=2, Generate Fake Small Lightmaps=3, Generated From Match Data=4, Only A Checker Board=5, Surface To Triangle Mapping Pruned=6, Faked Lightmap Tag For Cache Build=7, Optimized For Less DP All=8, Floating Shadows Enabled=9, Atlas Unrefined Packing=10, Atlas Repacked=11, Using Simplified Irradiance Lighting=12, Disable Shadow Geometry=13, Disable Hybrid Refinement=14]
    constexpr std::size_t k_BspReferenceIndex                           = 0x2;  // int16.
    constexpr std::size_t k_ChecksumFromStructureBsp                    = 0x4;  // int32.
    constexpr std::size_t k_Unknown                                     = 0x8;  // TagReference.
    constexpr std::size_t k_Brightness                                  = 0x18;  // float32.
    constexpr std::size_t k_PrimaryMap                                  = 0x1C;  // TagReference.
    constexpr std::size_t k_IntensityMap                                = 0x2C;  // TagReference.
    constexpr std::size_t k_Unknown_2                                   = 0x3C;  // undefined.
    constexpr std::size_t k_Unknown_3                                   = 0x40;  // undefined.
    constexpr std::size_t k_Unknown_4                                   = 0x44;  // undefined.
    namespace UnknownA
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x48;
        constexpr std::size_t k_EntrySize = 0x4;

        constexpr std::size_t k_VertexBufferIndex                           = 0x0;  // int16.
        constexpr std::size_t k_Unknown                                     = 0x2;  // int16.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace UnknownB
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x54;
        constexpr std::size_t k_EntrySize = 0x8;

        constexpr std::size_t k_Unknown                                     = 0x0;  // int32.
        constexpr std::size_t k_Unknown_2                                   = 0x4;  // undefined.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace InstancedGeometry
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x60;
        constexpr std::size_t k_EntrySize = 0xC;

        constexpr std::size_t k_Unknown                                     = 0x0;  // int16.
        constexpr std::size_t k_UnknownAIndex                               = 0x2;  // int16.
        constexpr std::size_t k_ColoringIndex                               = 0x4;  // int16.
        constexpr std::size_t k_Unknown_2                                   = 0x6;  // int16.
        constexpr std::size_t k_Unknown_3                                   = 0x8;  // uint32.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace Colors
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x6C;
        constexpr std::size_t k_EntrySize = 0x24;

        constexpr std::size_t k_Unknown                                     = 0x0;  // float32.
        constexpr std::size_t k_Unknown_2                                   = 0x4;  // float32.
        constexpr std::size_t k_Red                                         = 0x8;  // float32.
        constexpr std::size_t k_Blue                                        = 0xC;  // float32.
        constexpr std::size_t k_Unknown_3                                   = 0x10;  // float32.
        constexpr std::size_t k_Unknown_4                                   = 0x14;  // int32.
        constexpr std::size_t k_Unknown_5                                   = 0x18;  // float32.
        constexpr std::size_t k_Unknown_6                                   = 0x1C;  // float32.
        constexpr std::size_t k_Unknown_7                                   = 0x20;  // uint32.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    constexpr std::size_t k_Flags_2                                     = 0x78;  // flags32. [Processed=0, Available=1, Has Valid Budgets (Really)=2, Manual Resource Creation=3, Keep Raw Geometry=4, Don't Use Compressed Vertex Positions=5]
    namespace Meshes
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x7C;
        constexpr std::size_t k_EntrySize = 0x5C;

        namespace Parts
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x0;
            constexpr std::size_t k_EntrySize = 0x18;

            constexpr std::size_t k_RenderMaterialIndex                         = 0x0;  // int16.
            constexpr std::size_t k_TransparentSortingIndex                     = 0x2;  // int16.
            constexpr std::size_t k_IndexStart                                  = 0x4;  // uint32.
            constexpr std::size_t k_IndexCount                                  = 0x8;  // uint32.
            constexpr std::size_t k_SubpartStart                                = 0xC;  // int16.
            constexpr std::size_t k_SubpartCount                                = 0xE;  // uint16.
            constexpr std::size_t k_PartType                                    = 0x10;  // int8.
            constexpr std::size_t k_SpecializedRender                           = 0x11;  // enum8. [None=0x0, Fail=0x1, Fur=0x2, Fur Stencil=0x3, Decal=0x4, Shield=0x5, Water=0x6, Lightmap Only=0x7, Hologram=0x8]
            constexpr std::size_t k_PartFlags                                   = 0x12;  // flags16. [Is Water Surface=0, Per Vertex Lightmap Part=1, Debug Flag Instance Part=2, Subparts Has Uberlights Info=3, Draw Cull Distance Medium=4, Draw Cull Distance Close=5, Draw Cull Rendering Shields=6, Cannot Single Pass Render=7, Is Transparent=8, Cannot Two Pass=9, Transparent Should Output Depth For DOF=10, Do Not Include In Static Lightmap=11, Do Not Include In PVS Generation=12, Draw Cull Rendering Active Camo=13]
            constexpr std::size_t k_BudgetVertexCount                           = 0x14;  // uint16.
            constexpr std::size_t k_Tessellation                                = 0x16;  // enum16. [None=0x0, Fixed x4 Faces=0x1, Fixed x9 Faces=0x2, Fixed x36 Faces=0x3]

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        namespace Subparts
        {
            // Array data.
            constexpr std::size_t k_Base      = 0xC;
            constexpr std::size_t k_EntrySize = 0x10;

            constexpr std::size_t k_IndexStart                                  = 0x0;  // uint32.
            constexpr std::size_t k_IndexCount                                  = 0x4;  // uint32.
            constexpr std::size_t k_PartIndex                                   = 0x8;  // int16.
            constexpr std::size_t k_BudgetVertexCount                           = 0xA;  // uint16.
            constexpr std::size_t k_AnalyticalLightIndex                        = 0xC;  // uint32.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        constexpr std::size_t k_VertexBufferIndex1                          = 0x18;  // int16.
        constexpr std::size_t k_VertexBufferIndex2                          = 0x1A;  // int16.
        constexpr std::size_t k_VertexBufferIndex3                          = 0x1C;  // int16.
        constexpr std::size_t k_VertexBufferIndex4                          = 0x1E;  // int16.
        constexpr std::size_t k_VertexBufferIndex5                          = 0x20;  // int16.
        constexpr std::size_t k_VertexBufferIndex6                          = 0x22;  // int16.
        constexpr std::size_t k_VertexBufferIndex7                          = 0x24;  // int16.
        constexpr std::size_t k_VertexBufferIndex8                          = 0x26;  // int16.
        constexpr std::size_t k_IndexBufferIndex                            = 0x28;  // int16.
        constexpr std::size_t k_IndexBufferTessellationIndex                = 0x2A;  // int16.
        constexpr std::size_t k_MeshFlags                                   = 0x2C;  // flags16. [Mesh Has Vertex Color=0, Use Region Index For Sorting=1, Use Vertex Buffers For Indices=2, Mesh Has Per-Instance Lighting (Do Not Modify)=3, Mesh Is Unindexed (Do Not Modify)=4, Subpart Were Merged=5, Mesh Has Fur=6, Mesh Has Decal=7, Mesh Doesn't Use Compressed Position=8, Use Uncompressed Vertex Format=9, Bit 10=10, Bit 11=11, Bit 12=12, Bit 13=13, Bit 14=14, Bit 15=15]
        constexpr std::size_t k_RigidNodeIndex                              = 0x2E;  // int8.
        constexpr std::size_t k_VertexType                                  = 0x2F;  // enum8. [World=0x0, Rigid=0x1, Skinned=0x2, Particle Model=0x3, Flat World=0x4, Flat Rigid=0x5, Flat Skinned=0x6, Screen=0x7, Debug=0x8, Transparent=0x9, Particle=0xA, Contrail=0xB, Light Volume=0xC, Chud Simple=0xD, Chud Fancy=0xE, Decorator=0xF, Tiny Position=0x10, Patchy Fog=0x11, Water=0x12, Ripple=0x13, Implicit=0x14, Beam=0x15, World Tessellated=0x16, Rigid Tessellated=0x17, Skinned Tessellated=0x18, Shader Cache=0x19, Structure Instance Imposter=0x1A, Object Imposter=0x1B, Rigid Compressed=0x1C, Skinned Compressed=0x1D, Light Volume Precompiled=0x1E]
        constexpr std::size_t k_PrtType                                     = 0x30;  // enum8. [None=0x0, Ambient=0x1, Linear=0x2, Quadratic=0x3]
        constexpr std::size_t k_LightingPolicy                              = 0x31;  // enum8. [Vertex Color=0x0, Single Probe=0x1, PRT Ambient=0x2]
        constexpr std::size_t k_IndexBufferType                             = 0x32;  // enum8. [Point List=0x0, Line List=0x1, Line Strip=0x2, Triangle List=0x3, Triangle Fan=0x4, Triangle Strip=0x5]
        constexpr std::size_t k_Unknown                                     = 0x33;  // int8.
        namespace InstanceBuckets
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x34;
            constexpr std::size_t k_EntrySize = 0x10;

            constexpr std::size_t k_MeshIndex                                   = 0x0;  // int16.
            constexpr std::size_t k_DefinitionIndex                             = 0x2;  // int16.
            namespace Instances
            {
                // Array data.
                constexpr std::size_t k_Base      = 0x4;
                constexpr std::size_t k_EntrySize = 0x2;

                constexpr std::size_t k_InstanceIndex                               = 0x0;  // int16.

                constexpr std::size_t EntryOffset(std::size_t index)
                {
                    return k_Base + (index * k_EntrySize);
                }
            }


            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        namespace WaterIndicesStart
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x40;
            constexpr std::size_t k_EntrySize = 0x2;

            constexpr std::size_t k_Short                                       = 0x0;  // int16.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        constexpr std::size_t k_BoundingRadius                              = 0x4C;  // float32.
        constexpr std::size_t k_BoundingOffset                              = 0x50;  // point3.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace CompressionInfo
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x88;
        constexpr std::size_t k_EntrySize = 0x34;

        constexpr std::size_t k_CompressionFlags                            = 0x0;  // flags16. [Compressed Position=0, Compressed Texcoord=1, Compression Optimized=2]
        constexpr std::size_t k_Unknown                                     = 0x2;  // int16.
        constexpr std::size_t k_PositionBoundsX                             = 0x4;  // rangef.
        constexpr std::size_t k_PositionBoundsY                             = 0xC;  // rangef.
        constexpr std::size_t k_PositionBoundsZ                             = 0x14;  // rangef.
        constexpr std::size_t k_TexcoordBoundsX                             = 0x1C;  // rangef.
        constexpr std::size_t k_TexcoordBoundsY                             = 0x24;  // rangef.
        constexpr std::size_t k_Unknown_2                                   = 0x2C;  // float32.
        constexpr std::size_t k_Unknown_3                                   = 0x30;  // float32.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace PartSortingPosition
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x94;
        constexpr std::size_t k_EntrySize = 0x30;

        constexpr std::size_t k_Plane                                       = 0x0;  // plane3.
        constexpr std::size_t k_Position                                    = 0x10;  // point3.
        constexpr std::size_t k_Radius                                      = 0x1C;  // float32.
        constexpr std::size_t k_NodeIndex1                                  = 0x20;  // int8.
        constexpr std::size_t k_NodeIndex2                                  = 0x21;  // int8.
        constexpr std::size_t k_NodeIndex3                                  = 0x22;  // int8.
        constexpr std::size_t k_NodeIndex4                                  = 0x23;  // int8.
        constexpr std::size_t k_NodeWeight1                                 = 0x24;  // float32.
        constexpr std::size_t k_NodeWeight2                                 = 0x28;  // float32.
        constexpr std::size_t k_NodeWeight3                                 = 0x2C;  // float32.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace UserData
    {
        // Array data.
        constexpr std::size_t k_Base      = 0xA0;
        constexpr std::size_t k_EntrySize = 0x18;

        constexpr std::size_t k_DataType                                    = 0x0;  // enum8. [PRT Info=0x0]
        constexpr std::size_t k_DataCount                                   = 0x1;  // int8.
        constexpr std::size_t k_DataSize                                    = 0x2;  // uint16.
        constexpr std::size_t k_UserData                                    = 0x4;  // DataReference.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace PerMeshTemporary
    {
        // Array data.
        constexpr std::size_t k_Base      = 0xAC;
        constexpr std::size_t k_EntrySize = 0x54;

        namespace RawVertices
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x0;
            constexpr std::size_t k_EntrySize = 0x60;

            constexpr std::size_t k_Position                                    = 0x0;  // point3.
            constexpr std::size_t k_Texcoord                                    = 0xC;  // point2.
            constexpr std::size_t k_Normal                                      = 0x14;  // point3.
            constexpr std::size_t k_Binormal                                    = 0x20;  // point3.
            constexpr std::size_t k_Tangent                                     = 0x2C;  // point3.
            constexpr std::size_t k_LightmapTexcoord                            = 0x38;  // point2.
            constexpr std::size_t k_NodeIndex1                                  = 0x40;  // uint8.
            constexpr std::size_t k_NodeIndex2                                  = 0x41;  // uint8.
            constexpr std::size_t k_NodeIndex3                                  = 0x42;  // uint8.
            constexpr std::size_t k_NodeIndex4                                  = 0x43;  // uint8.
            constexpr std::size_t k_NodeWeight1                                 = 0x44;  // float32.
            constexpr std::size_t k_NodeWeight2                                 = 0x48;  // float32.
            constexpr std::size_t k_NodeWeight3                                 = 0x4C;  // float32.
            constexpr std::size_t k_NodeWeight4                                 = 0x50;  // float32.
            constexpr std::size_t k_VertexColor                                 = 0x54;  // point3.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        namespace RawIndices
        {
            // Array data.
            constexpr std::size_t k_Base      = 0xC;
            constexpr std::size_t k_EntrySize = 0x2;

            constexpr std::size_t k_Short                                       = 0x0;  // int16.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        namespace RawIndices32
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x18;
            constexpr std::size_t k_EntrySize = 0x4;

            constexpr std::size_t k_Int                                         = 0x0;  // int32.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        namespace RawWaterData
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x24;
            constexpr std::size_t k_EntrySize = 0x18;

            namespace RawWaterIndices
            {
                // Array data.
                constexpr std::size_t k_Base      = 0x0;
                constexpr std::size_t k_EntrySize = 0x2;

                constexpr std::size_t k_Short                                       = 0x0;  // int16.

                constexpr std::size_t EntryOffset(std::size_t index)
                {
                    return k_Base + (index * k_EntrySize);
                }
            }

            namespace RawWaterVertices
            {
                // Array data.
                constexpr std::size_t k_Base      = 0xC;
                constexpr std::size_t k_EntrySize = 0x8;

                constexpr std::size_t k_BaseTexcoord                                = 0x0;  // point2.

                constexpr std::size_t EntryOffset(std::size_t index)
                {
                    return k_Base + (index * k_EntrySize);
                }
            }


            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        namespace RawImposterData
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x30;
            constexpr std::size_t k_EntrySize = 0x4C;

            constexpr std::size_t k_Diffuse                                     = 0x0;  // colorf.
            constexpr std::size_t k_Ambient                                     = 0xC;  // colorf.
            constexpr std::size_t k_Specular                                    = 0x18;  // colorf.
            constexpr std::size_t k_Shininess                                   = 0x24;  // float32.
            constexpr std::size_t k_Alpha                                       = 0x28;  // float32.
            constexpr std::size_t k_ChangeColorTintOfDiffuse                    = 0x2C;  // colorf.
            constexpr std::size_t k_ChangeColorTintOfSpecular                   = 0x3C;  // colorf.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        namespace RawInstanceImposterVerts
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x3C;
            constexpr std::size_t k_EntrySize = 0x14;

            constexpr std::size_t k_Position                                    = 0x0;  // vector3.
            constexpr std::size_t k_Color                                       = 0xC;  // uint32.
            constexpr std::size_t k_HdrScaler                                   = 0x10;  // float32.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }

        constexpr std::size_t k_PerVertexLightingVertexSize                 = 0x48;  // int32.
        constexpr std::size_t k_ParameterizedTextureWidth                   = 0x4C;  // int16.
        constexpr std::size_t k_ParameterizedTextureHeight                  = 0x4E;  // int16.
        constexpr std::size_t k_Flags                                       = 0x50;  // flags32. [Indices Are Triangle Strips=0, Indices Are Triangle Lists=1, Indices Are Quad Lists=2]

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    constexpr std::size_t k_Unknown_5                                   = 0xB8;  // undefined.
    constexpr std::size_t k_Unknown_6                                   = 0xBC;  // undefined.
    constexpr std::size_t k_Unknown_7                                   = 0xC0;  // undefined.
    namespace PerMeshNodeMap
    {
        // Array data.
        constexpr std::size_t k_Base      = 0xC4;
        constexpr std::size_t k_EntrySize = 0xC;

        namespace NodeMap
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x0;
            constexpr std::size_t k_EntrySize = 0x1;

            constexpr std::size_t k_NodeIndex                                   = 0x0;  // int8.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }


        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace PerMeshSubpartVisibility
    {
        // Array data.
        constexpr std::size_t k_Base      = 0xD0;
        constexpr std::size_t k_EntrySize = 0xC;

        namespace BoundingSphere
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x0;
            constexpr std::size_t k_EntrySize = 0x30;

            constexpr std::size_t k_Plane                                       = 0x0;  // plane3.
            constexpr std::size_t k_Position                                    = 0x10;  // point3.
            constexpr std::size_t k_Radius                                      = 0x1C;  // float32.
            constexpr std::size_t k_NodeIndex1                                  = 0x20;  // uint8.
            constexpr std::size_t k_NodeIndex2                                  = 0x21;  // uint8.
            constexpr std::size_t k_NodeIndex3                                  = 0x22;  // uint8.
            constexpr std::size_t k_NodeIndex4                                  = 0x23;  // uint8.
            constexpr std::size_t k_NodeWeight1                                 = 0x24;  // float32.
            constexpr std::size_t k_NodeWeight2                                 = 0x28;  // float32.
            constexpr std::size_t k_NodeWeight3                                 = 0x2C;  // float32.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }


        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    constexpr std::size_t k_BlockHere1                                  = 0xDC;  // uint32.
    constexpr std::size_t k_BlockHere1_2                                = 0xE0;  // uint32.
    constexpr std::size_t k_BlockHere1_3                                = 0xE4;  // uint32.
    constexpr std::size_t k_BlockHere2                                  = 0xE8;  // uint32.
    constexpr std::size_t k_BlockHere2_2                                = 0xEC;  // uint32.
    constexpr std::size_t k_BlockHere2_3                                = 0xF0;  // uint32.
    namespace PerInstanceLightmapTexcoords
    {
        // Array data.
        constexpr std::size_t k_Base      = 0xF4;
        constexpr std::size_t k_EntrySize = 0x2;

        constexpr std::size_t k_VertexBufferIndex                           = 0x0;  // int16.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace WaterBoundingBox
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x100;
        constexpr std::size_t k_EntrySize = 0x1C;

        constexpr std::size_t k_MeshIndex                                   = 0x0;  // int16.
        constexpr std::size_t k_PartIndex                                   = 0x2;  // int16.
        constexpr std::size_t k_PositionBounds0                             = 0x4;  // point3.
        constexpr std::size_t k_PositionBounds1                             = 0x10;  // point3.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    constexpr std::size_t k_ZoneAssetDatum                              = 0x10C;  // datum.
    constexpr std::size_t k_UselessPadding                              = 0x110;  // int32.
    namespace ConstantBufferInterop
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x114;
        constexpr std::size_t k_EntrySize = 0xC;

        constexpr std::size_t k_InteropPointer                              = 0x0;  // uint32.
        constexpr std::size_t k_Unknown                                     = 0x4;  // undefined.
        constexpr std::size_t k_UselessPadding                              = 0x8;  // int32.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace Unknown
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x120;
        constexpr std::size_t k_EntrySize = 0xC;

        namespace Unknown
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x0;
            constexpr std::size_t k_EntrySize = 0x4;

            constexpr std::size_t k_Unknown                                     = 0x0;  // undefined.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }


        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace Airprobes
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x12C;
        constexpr std::size_t k_EntrySize = 0x38;

        constexpr std::size_t k_AirprobePosition                            = 0x0;  // point3.
        constexpr std::size_t k_AirprobeName                                = 0xC;  // StringID.
        constexpr std::size_t k_Unknown                                     = 0x10;  // undefined.
        constexpr std::size_t k_Unknown_2                                   = 0x14;  // undefined.
        constexpr std::size_t k_Unknown_3                                   = 0x18;  // undefined.
        constexpr std::size_t k_Unknown_4                                   = 0x1C;  // undefined.
        constexpr std::size_t k_Unknown_5                                   = 0x20;  // undefined.
        constexpr std::size_t k_Unknown_6                                   = 0x24;  // undefined.
        constexpr std::size_t k_Unknown_7                                   = 0x28;  // undefined.
        constexpr std::size_t k_Unknown_8                                   = 0x2C;  // undefined.
        constexpr std::size_t k_Unknown_9                                   = 0x30;  // undefined.
        constexpr std::size_t k_Unknown_10                                  = 0x34;  // undefined.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace Unknown_2
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x138;
        constexpr std::size_t k_EntrySize = 0x2C;

        constexpr std::size_t k_Unknown                                     = 0x0;  // undefined.
        constexpr std::size_t k_Unknown_2                                   = 0x4;  // undefined.
        constexpr std::size_t k_Unknown_3                                   = 0x8;  // undefined.
        constexpr std::size_t k_Unknown_4                                   = 0xC;  // undefined.
        constexpr std::size_t k_Unknown_5                                   = 0x10;  // undefined.
        constexpr std::size_t k_Unknown_6                                   = 0x14;  // undefined.
        constexpr std::size_t k_Unknown_7                                   = 0x18;  // undefined.
        constexpr std::size_t k_Unknown_8                                   = 0x1C;  // undefined.
        constexpr std::size_t k_Unknown_9                                   = 0x20;  // undefined.
        constexpr std::size_t k_Unknown_10                                  = 0x24;  // undefined.
        constexpr std::size_t k_Unknown_11                                  = 0x28;  // undefined.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace Unknown_3
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x144;
        constexpr std::size_t k_EntrySize = 0x2C;

        constexpr std::size_t k_Unknown                                     = 0x0;  // undefined.
        constexpr std::size_t k_Unknown_2                                   = 0x4;  // undefined.
        constexpr std::size_t k_Unknown_3                                   = 0x8;  // undefined.
        constexpr std::size_t k_Unknown_4                                   = 0xC;  // undefined.
        constexpr std::size_t k_Unknown_5                                   = 0x10;  // undefined.
        constexpr std::size_t k_Unknown_6                                   = 0x14;  // undefined.
        constexpr std::size_t k_Unknown_7                                   = 0x18;  // undefined.
        constexpr std::size_t k_Unknown_8                                   = 0x1C;  // undefined.
        namespace Unknown
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x20;
            constexpr std::size_t k_EntrySize = 0x30;

            constexpr std::size_t k_Unknown                                     = 0x0;  // undefined.
            constexpr std::size_t k_Unknown_2                                   = 0x4;  // undefined.
            constexpr std::size_t k_Unknown_3                                   = 0x8;  // undefined.
            constexpr std::size_t k_Unknown_4                                   = 0xC;  // undefined.
            constexpr std::size_t k_Unknown_5                                   = 0x10;  // undefined.
            constexpr std::size_t k_Unknown_6                                   = 0x14;  // undefined.
            constexpr std::size_t k_Unknown_7                                   = 0x18;  // undefined.
            constexpr std::size_t k_Unknown_8                                   = 0x1C;  // undefined.
            constexpr std::size_t k_Unknown_9                                   = 0x20;  // undefined.
            constexpr std::size_t k_Unknown_10                                  = 0x24;  // undefined.
            constexpr std::size_t k_Unknown_11                                  = 0x28;  // undefined.
            constexpr std::size_t k_Unknown_12                                  = 0x2C;  // undefined.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }


        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    constexpr std::size_t k_Unknown_8                                   = 0x150;  // undefined.
    constexpr std::size_t k_Unknown_9                                   = 0x154;  // undefined.
    constexpr std::size_t k_Unknown_10                                  = 0x158;  // undefined.
    namespace SelfTrack
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x15C;
        constexpr std::size_t k_EntrySize = 0x240;

        constexpr std::size_t k_Time                                        = 0x0;  // ascii.
        constexpr std::size_t k_Machine                                     = 0x20;  // ascii.
        constexpr std::size_t k_Version                                     = 0x40;  // ascii.
        constexpr std::size_t k_Command                                     = 0x140;  // ascii.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

}
