// Auto-generated by TagTranspiler (GenerateOffset.py)
// Source XML: play.xml
// Base size: 0x50.

export module Map.Tag.Type:Play.Offset;

import std;

export namespace Map::Tag::Type::Play::Offset
{
    namespace CompressionCodecs
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x0;
        constexpr std::size_t k_EntrySize = 0x10;

        constexpr std::size_t k_Guid                                        = 0x0;  // hexstring.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace ExternalCacheReferences
    {
        // Array data.
        constexpr std::size_t k_Base      = 0xC;
        constexpr std::size_t k_EntrySize = 0x108;

        constexpr std::size_t k_MapPath                                     = 0x0;  // ascii.
        constexpr std::size_t k_Unknown                                     = 0x100;  // int16.
        constexpr std::size_t k_Unknown_2                                   = 0x102;  // int16.
        constexpr std::size_t k_Unknown_3                                   = 0x104;  // undefined.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace RawPages
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x18;
        constexpr std::size_t k_EntrySize = 0x58;

        constexpr std::size_t k_Salt                                        = 0x0;  // int16.
        constexpr std::size_t k_Flags                                       = 0x2;  // int8.
        constexpr std::size_t k_CompressionCodecIndex                       = 0x3;  // int8.
        constexpr std::size_t k_SharedCacheIndex                            = 0x4;  // int16.
        constexpr std::size_t k_PrecacheIndex                               = 0x6;  // int16.
        constexpr std::size_t k_BlockOffset                                 = 0x8;  // uint32.
        constexpr std::size_t k_CompressedBlockSize                         = 0xC;  // uint32.
        constexpr std::size_t k_UncompressedBlockSize                       = 0x10;  // uint32.
        constexpr std::size_t k_CrcChecksum                                 = 0x14;  // int32.
        constexpr std::size_t k_EntireBufferHash                            = 0x18;  // hexstring.
        constexpr std::size_t k_FirstChunkHash                              = 0x2C;  // hexstring.
        constexpr std::size_t k_LastChunkHash                               = 0x40;  // hexstring.
        constexpr std::size_t k_BlockAssetCount                             = 0x54;  // int16.
        constexpr std::size_t k_UnknownIndex                                = 0x56;  // int16.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace Sizes
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x24;
        constexpr std::size_t k_EntrySize = 0x10;

        constexpr std::size_t k_OverallSize                                 = 0x0;  // int32.
        namespace Parts
        {
            // Array data.
            constexpr std::size_t k_Base      = 0x4;
            constexpr std::size_t k_EntrySize = 0x8;

            constexpr std::size_t k_Offset                                      = 0x0;  // int32.
            constexpr std::size_t k_Size                                        = 0x4;  // int32.

            constexpr std::size_t EntryOffset(std::size_t index)
            {
                return k_Base + (index * k_EntrySize);
            }
        }


        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace Unknown
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x30;
        constexpr std::size_t k_EntrySize = 0xC;

        constexpr std::size_t k_Unknown                                     = 0x0;  // int32.
        constexpr std::size_t k_Unknown_2                                   = 0x4;  // int32.
        constexpr std::size_t k_Unknown_3                                   = 0x8;  // int32.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    namespace Segments
    {
        // Array data.
        constexpr std::size_t k_Base      = 0x3C;
        constexpr std::size_t k_EntrySize = 0x10;

        constexpr std::size_t k_PrimaryPageIndex                            = 0x0;  // int16.
        constexpr std::size_t k_SecondaryPageIndex                          = 0x2;  // int16.
        constexpr std::size_t k_PrimarySegmentOffset                        = 0x4;  // int32.
        constexpr std::size_t k_SecondarySegmentOffset                      = 0x8;  // int32.
        constexpr std::size_t k_PrimarySizeIndex                            = 0xC;  // int16.
        constexpr std::size_t k_SecondarySizeIndex                          = 0xE;  // int16.

        constexpr std::size_t EntryOffset(std::size_t index)
        {
            return k_Base + (index * k_EntrySize);
        }
    }

    constexpr std::size_t k_HeaderPrecacheCount                         = 0x48;  // int32.
    constexpr std::size_t k_Unknown                                     = 0x4C;  // undefined.
}
